<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.unistundenplan">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:roundIcon="@drawable/hfo_university_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.UniStundenplan">
        <activity
            android:name=".View.LessonDetails.DetailsTabbedActivity"
            android:exported="false"
            android:label="@string/title_activity_details_tabbed"
            android:theme="@style/Theme.UniStundenplan.NoActionBar" />
        <activity
            android:name=".View.Schedule.ScheduleTabbedActivity"
            android:exported="true"
            android:label="@string/title_activity_tabbed"
            android:theme="@style/Theme.UniStundenplan.NoActionBar" />
        <activity
            android:name=".View.Init.InitSettingsActivity"
            android:exported="false" />
        <activity
            android:name=".View.Init.SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>